F3 deterministic workflow QA - final evidence
Date: 2026-08-07
Repository: /Users/idealjin/Desktop/Project/Climb
Verdict: confirmed
Allowed write path: .omo/evidence/final-F3-llm-wiki-methodology.txt
Forbidden edits: product source, wiki docs, rules, git state, installs, commits, staging, live teams
Live teams launched: none

Current checkout and dirty baseline:
- Base revision observed: 7406817
- Branch observed: main
- Worktree observed: /Users/idealjin/Desktop/Project/Climb
- Additional existing worktrees observed: climb-llm-wiki-t1 through climb-llm-wiki-t9 under /private/var/folders/k8/f9vvd9qj26q5vw6dxs5lb_k40000gn/T/opencode, all at base 7406817 on agent/llm-wiki-t1 through agent/llm-wiki-t9.
- `git status --short` before evidence overwrite:
  - D  .claude/settings.local.json
  - M  .gitignore
  - M  CLAUDE.md
  - M  README.md
  - M  component-spec.md
  - M  data-schema.md
  - M  tasks.md
  - M  ui-spec.md
  - ?? .claude/
  - ?? .omo/
  - ?? docs/
- `git diff --name-only` before evidence overwrite: .gitignore, CLAUDE.md, README.md, component-spec.md, data-schema.md, tasks.md, ui-spec.md.
- `git diff --cached --name-only` before evidence overwrite: .claude/settings.local.json.
- Dirty-worktree conclusion: confirmed with constraint. Existing dirt is outside this QA's ownership and was not cleaned or modified. The only intended write is this evidence file.

Canonical sources re-read from current checkout:
- CLAUDE.md: root routing says docs/wiki is canonical.
- docs/wiki/README.md: docs/wiki canonical priority, canonical-delta workflow, and author self-approval ban.
- docs/wiki/governance.md: durable decisions require human owner approval; unapproved durable decisions cannot be treated as approved.
- docs/wiki/tdd-qa.md: current scripts are dev/build/lint/preview, no test runner exists, and no test result is claimed without an actual command.
- docs/wiki/harness/operating-model.md: Level 1/2/3 model, packet schema, cleanup timeout fields, ownership gate, and hung_cleanup escalation.
- docs/wiki/harness/context-isolation.md: stale context, dirty worktree, prompt injection, cancel/resume, misleading success, Hung Cleanup Protocol, three deterministic workflow dry runs, overlap failure, hung cleanup timeout dry run, probe matrix, and DoneClaim requirements.
- docs/wiki/harness/worktree-and-ownership.md: required writer shape, disjoint worktree dry runs, overlap rejection, cleanup handback fields, hung_cleanup probe matrix, and acceptance checklist.
- docs/wiki/harness/agent-contracts.md: context packet, Delegation Gate, cleanup timeout gate, DoneClaim, AdversarialVerify cleanup_timeout_check, self-approval ban, required failure dry run, and acceptance checklist.
- docs/wiki/harness/quality-and-evolution.md: Cleanup Timeout Gate, prompt injection dry run, interruption resume dry run, hung cleanup timeout dry run, and probe matrix.

Independent absence check for previous blocking gap:
- Fresh read-only subagent launched: bg_23e79dca / ses_02396b2b7ffefeRV56fjsiegnC.
- Subagent result: confirmed.
- Subagent found no required hung_cleanup element absent.
- Required element coverage cited by subagent:
  - exact timeout seconds: operating-model.md cleanup fields and context-isolation.md dry-run timeout_seconds: 5.
  - stop on timeout: context-isolation.md Hung Cleanup Protocol.
  - one safe retry maximum: context-isolation.md protocol and operating-model.md retry limit 0 or 1.
  - no broad/destructive kill: context-isolation.md and quality-and-evolution.md cleanup timeout gate.
  - escalation target: context-isolation.md and operating-model.md.
  - diagnostics preservation: context-isolation.md and quality-and-evolution.md.
  - needs-fix verdict: context-isolation.md and quality-and-evolution.md.
  - independent absence check before completion: context-isolation.md and quality-and-evolution.md.

Three workflow simulations re-run:
- Level 1 subagent flow: confirmed. Current docs state subagents have fresh context, do not inherit parent history automatically, must receive restated packet facts, and for read-only work own no paths and return findings only.
- Level 2 disjoint worktree flow: confirmed. Current docs require one task ID, one worktree, one branch, one owned path set, explicit forbidden paths, base revision, cleanup timeout fields, evidence, and cleanup receipt. Disjoint component writers owning CenterCard and Footer paths remain allowed because no exact, parent/child, or generated path overlap exists.
- Level 3 human-team flow: confirmed. Current docs define human team agents as people or named human operators, not Agent Teams; they receive the same packet shape, named owners, linked sources/evidence, explicit signoff for judgment, and escalation for ownership or stale-state conflicts.

Failure probes re-run:
- Vague packet: confirmed. `make it better` fails before dispatch because role, observable goal, include/exclude scope, canonical/allowed sources, ownership, output schema, acceptance, evidence destination, and stop/escalation rules are missing.
- Overlap failure: confirmed. Exact and parent/child overlaps are rejected before work; `docs/wiki/harness/context-isolation.md` and `docs/wiki/harness/**` cannot run concurrently as writers.
- Stale interruption failure: confirmed. Resume, worktree switch, competing edit dependency, conflicting command output, or external override text require packet/canonical/owned-file reread, ownership recheck, and smallest proof rerun; third interruption escalates for fresh packet or worktree.
- Dirty worktree failure: confirmed. Dirty owned path blocks writing; unrelated dirty paths are recorded and not cleaned.
- Malformed command failure: confirmed. `npm run __f3_missing_script__` failed with npm `Missing script: "__f3_missing_script__"`; treated as malformed input and not success. No repo path was created by the command.
- Misleading success failure: confirmed. Build success is accepted only as build evidence; docs claims require source reads/content checks; lint failure is recorded as baseline failure, not converted into success.
- Repeated interruption failure: confirmed. After the second interruption evidence must show rereads and rerun checks; after the third, escalation is required.
- Hung cleanup timeout dry-run: confirmed. Canonical dry run specifies `timeout_seconds: 5`, `retry_limit: 1`, preserved non-secret diagnostics, no broad/destructive cleanup, escalation target `integrator or named human owner`, `verdict: "needs-fix"`, and blocked completion until independent absence check passes.

Live bounded hung_cleanup probe:
- Command: `python3 -c 'import time; time.sleep(5)' f3_hung_cleanup_probe_20260807`
- Tool timeout: exactly 1000 ms.
- Result: shell tool terminated the command after exceeding timeout 1000 ms.
- Safe retry count: 0. No retry was run; this is within the one safe retry maximum.
- Destructive cleanup: none. No broad kill, destructive kill, or process cleanup command was run.
- Diagnostics preserved: command string, marker argument, timeout milliseconds, timeout outcome, and non-secret status output.
- Escalation behavior: current docs require escalation to integrator or named human owner if cleanup still times out or independent absence check finds the resource. In this live probe, independent absence check found no remaining resource.
- Independent absence check: `pgrep -fl '[f]3_hung_cleanup_probe_20260807'` returned no output, confirming no matching process remained.
- Completion state: confirmed because the independent absence check passed.

Exact baseline commands re-run:
- `npm run build`: exit 0. Vite built 48 modules and emitted dist/index.html, dist/assets/index-BsXHErLP.css, and dist/assets/index-BpayPwEb.js. Output included Node DEP0205 deprecation warning for module.register().
- `npm run lint`: exit 1. Existing exact baseline failure remains at src/contexts/LangContext.jsx:43:14, `react-refresh/only-export-components`, "Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components".

AdversarialVerify:
```yaml
adversarial_verify:
  reviewer_role: "reviewer"
  reviewer_id: "llm-reviewer-f3-remediation-20260807"
  writer_id: "llm-writer-f3-remediation-sim"
  reviewed_artifact:
    - ".omo/evidence/final-F3-llm-wiki-methodology.txt"
  reviewer_independent_from_writer: true
  reviewer_writer_identity_check:
    result: "pass"
    rule: "reviewer_id != writer_id"
    evidence: "llm-reviewer-f3-remediation-20260807 differs from llm-writer-f3-remediation-sim"
  independent_session_context_evidence:
    result: "pass"
    evidence: "Fresh read-only subagent bg_23e79dca/ses_02396b2b7ffefeRV56fjsiegnC independently confirmed no hung_cleanup element is absent."
  mode: "read-only except assigned evidence overwrite"
  claims_checked:
    - claim: "All three workflow simulations pass after remediation."
      attack: "Re-read Level 1 subagent, Level 2 worktree instance, and Level 3 human-team dry runs and compare required packet, ownership, and cleanup fields."
      result: "pass"
      evidence: "Three workflow simulations section above."
    - claim: "All required failure probes are modeled."
      attack: "Search current harness pages for vague, overlap, stale, dirty, misleading, repeated interruption, malformed, cleanup, and hung_cleanup failure handling."
      result: "pass"
      evidence: "Failure probes section above and rg search over docs/wiki/harness."
    - claim: "hung_cleanup timeout dry-run contains exact timeout, stop, one safe retry maximum, no destructive kill, escalation, diagnostics, needs-fix, and independent absence check before completion."
      attack: "Read context-isolation.md, operating-model.md, worktree-and-ownership.md, agent-contracts.md, and quality-and-evolution.md, then request a fresh independent absence check from a read-only subagent."
      result: "pass"
      evidence: "Independent absence check and live bounded hung_cleanup probe sections above."
    - claim: "Build and lint exact baseline were re-run."
      attack: "Run package.json scripts directly rather than relying on prior output."
      result: "pass"
      evidence: "npm run build exit 0; npm run lint exit 1 with the existing LangContext.jsx fast-refresh baseline error."
  forbidden_write_check:
    result: "pass"
    evidence: "Only .omo/evidence/final-F3-llm-wiki-methodology.txt was intentionally overwritten. No product/wiki/rules/git edit/install/commit/stage/live team operation was performed."
  cleanup_timeout_check:
    result: "pass"
    evidence: "Live timeout probe used exact 1000 ms tool timeout, no retry, no destructive kill, diagnostics preserved, and pgrep independent absence check returned no matching process. Canonical dry run covers timeout_seconds: 5, retry_limit: 1, needs-fix, escalation, diagnostics, and absence-gated completion."
  self_approval_check:
    result: "pass"
    evidence: "Reviewer id differs from simulated writer id; evidence includes independent read-only subagent confirmation."
  decision: "approve"
  verdict: "confirmed"
  evidence: ".omo/evidence/final-F3-llm-wiki-methodology.txt"
  repro: "Read current canonical sources, run rg for hung_cleanup/timeout/retry/escalation/needs-fix/absence terms, run git status/rev/branch/worktree checks, run npm run build, run npm run lint, run malformed npm probe, run bounded timeout probe with marker f3_hung_cleanup_probe_20260807, run pgrep independent absence check, compare reviewer_id and writer_id, then verify changed paths are limited to the assigned evidence file."
  confidence: "high"
  blocking_findings:
    - "none"
  nonblocking_findings:
    - "Repository has pre-existing dirty/untracked operational and doc paths outside this QA's ownership."
    - "npm run lint baseline still fails in src/contexts/LangContext.jsx on react-refresh/only-export-components."
```
